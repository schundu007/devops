"""DC-NET-13 Reachability Check — your attempt.

Run your code against the tests:
    make try CHIP=05-networking/DC-NET-13-reachability-check
"""
from __future__ import annotations


def can_reach(n: int, links: list[list[int]], source: int, target: int) -> bool:
    """Return True if traffic from `source` can reach `target`.

    Nodes are 0..n-1. links[i] = [a, b] is an allowed two-way path between a and b.
    """
    # TODO
    raise NotImplementedError
