"""DC-NET-12 Failure Spread Timer — your attempt.  (added: the handbook has no stored starter)

Signature matches Source: Handbook #95 Rotting Oranges.
Run your code against the tests:
    make try CHIP=05-networking/DC-NET-12-failure-spread-timer
"""
from typing import List


class Solution:
    def orangesRotting(self, grid: List[List[int]]) -> int:
        """Minutes until no fresh cell remains, or -1 if some fresh cell is never reached."""
        # TODO
        raise NotImplementedError
