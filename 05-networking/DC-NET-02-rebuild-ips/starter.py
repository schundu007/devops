"""DC-NET-02 Rebuild IPs from Broken Logs — your attempt.

Run your code against the tests:
    make try CHIP=05-networking/DC-NET-02-rebuild-ips
"""
from __future__ import annotations


def restore_addresses(digits: str) -> list[str]:
    """Return every valid dotted IPv4 address that `digits` could have been.

    Insert exactly three dots into `digits` without reordering, adding or dropping any digit.
    Each of the four parts must be 0-255 with no leading zero ("0" alone is fine).
    Any order is accepted. Return [] if there is no valid address.
    """
    # TODO
    raise NotImplementedError
