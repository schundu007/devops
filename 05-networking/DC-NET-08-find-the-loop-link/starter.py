"""DC-NET-08 Find the Loop Link — your attempt.

Run your code against the tests:
    make try CHIP=05-networking/DC-NET-08-find-the-loop-link
"""
from __future__ import annotations


def find_loop_link(n: int, links: list[list[int]]) -> list[int]:
    """Return the one link to disable so the n switches form a loop-free tree.

    `links` is a loop-free tree over switches 1..n plus exactly one extra link.
    If several links could be removed, return the one that appears last in `links`.
    """
    # TODO
    raise NotImplementedError
