"""DC-NET-11 Cheapest Full Connectivity — your attempt.

Run your code against the tests:
    make try CHIP=05-networking/DC-NET-11-cheapest-full-connectivity
"""
from __future__ import annotations


def min_interconnect_cost(sites: list[list[int]]) -> int:
    """Return the least total cable length that connects every site.

    sites[i] = [x, y] is a site on the campus grid. A cable between two sites costs
    their Manhattan distance |x1 - x2| + |y1 - y2|. Any site may link to any other.
    """
    # TODO
    raise NotImplementedError
