"""DC-CAP-02 Backup Window Bandwidth — your attempt.

Run your code against the tests:
    make try CHIP=06-capacity/DC-CAP-02-backup-window-bandwidth
"""
from __future__ import annotations


def min_nightly_capacity(files: list[int], nights: int) -> int:
    """Return the smallest nightly transfer capacity (GB) that copies every file within `nights`.

    Files must be copied in the given order, and a file is never split across two nights.
    """
    # TODO
    raise NotImplementedError
