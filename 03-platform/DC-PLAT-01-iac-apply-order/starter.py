"""DC-PLAT-01 IaC Apply Order — your attempt.  (added: the handbook has no stored starter)

Signature matches Source: Handbook #96 Course Schedule II.
Run your code against the tests:
    make try CHIP=03-platform/DC-PLAT-01-iac-apply-order
"""
from typing import List


class Solution:
    def findOrder(self, numCourses: int, prerequisites: List[List[int]]) -> List[int]:
        """Any order of 0..numCourses-1 that puts b before a for every [a, b]; [] if there is a cycle."""
        # TODO
        raise NotImplementedError
