"""DC-PLAT-04 Job Queue with Cooldown — your attempt.  (added: the handbook has no stored starter)

Signature matches Source: Handbook #73 Task Scheduler (a module-level function there).
Run your code against the tests:
    make try CHIP=03-platform/DC-PLAT-04-job-cooldown-queue
"""
from typing import List


def leastInterval(tasks: List[str], n: int) -> int:
    """Fewest time slots to run every job, with n slots between two jobs of the same type."""
    # TODO
    raise NotImplementedError
