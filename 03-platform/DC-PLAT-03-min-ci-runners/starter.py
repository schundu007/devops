"""DC-PLAT-03 Minimum CI Runners — your attempt.  (added: the handbook has no stored starter)

Signature matches Source: Handbook #64 Meeting Rooms II (a module-level function there).
Run your code against the tests:
    make try CHIP=03-platform/DC-PLAT-03-min-ci-runners
"""
from typing import List


def minMeetingRooms(intervals: List[List[int]]) -> int:
    """Fewest runners so no job waits. A runner freed at t can start a job at t."""
    # TODO
    raise NotImplementedError
