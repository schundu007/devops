"""DC-PLAT-02 Circular Dependency Detector — your attempt.  (added: the handbook has no stored starter)

Signature matches Source: Handbook #45 Course Schedule (a module-level function there).
Run your code against the tests:
    make try CHIP=03-platform/DC-PLAT-02-circular-dependency
"""
from typing import List


def canFinish(numCourses: int, prerequisites: List[List[int]]) -> bool:
    """True if some order finishes every node, given [a, b] means b must finish before a."""
    # TODO
    raise NotImplementedError
