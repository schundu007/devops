"""DC-REL-02 Version Comparator — your attempt.

Run your code against the tests:
    make try CHIP=04-release/DC-REL-02-version-compare
"""
from __future__ import annotations


def compare_versions(a: str, b: str) -> int:
    """Compare two dotted versions such as "1.27.10" and "1.27.9".

    Each dot-separated part is a non-negative integer, possibly with leading zeros.
    A missing part counts as 0. Return 1 if a is newer, -1 if a is older, 0 if equal.
    """
    # TODO
    raise NotImplementedError
