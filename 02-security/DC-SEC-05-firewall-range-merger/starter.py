"""DC-SEC-05 Firewall Range Merger — your attempt.  (added: the handbook has no stored starter)

Signature matches Source: Handbook #61 Merge Intervals (a plain function, like the handbook's code).
Run your code against the tests:
    make try CHIP=02-security/DC-SEC-05-firewall-range-merger
"""
from typing import List


def merge(intervals: List[List[int]]) -> List[List[int]]:
    """Merge overlapping [start, end] intervals (shared endpoints overlap), sorted by start."""
    # TODO
    raise NotImplementedError
