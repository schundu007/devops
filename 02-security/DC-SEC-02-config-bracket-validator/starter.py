"""DC-SEC-02 Config Bracket Validator — your attempt.  (added: the handbook has no stored starter)

Signature matches Source: Handbook #16 Valid Parentheses (a plain function, like the handbook's code).
Run your code against the tests:
    make try CHIP=02-security/DC-SEC-02-config-bracket-validator
"""


def isValid(s: str) -> bool:
    """True if every (, [ and { in s is closed by the same type, in the right order."""
    # TODO
    raise NotImplementedError
