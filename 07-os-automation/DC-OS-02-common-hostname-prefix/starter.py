"""DC-OS-02 Common Hostname Prefix — your attempt.

Run your code against the tests:
    make try CHIP=07-os-automation/DC-OS-02-common-hostname-prefix
"""
from __future__ import annotations


def common_prefix(hosts: list[str]) -> str:
    """Return the longest string that every hostname starts with.

    Return "" if the list is empty or the hostnames share no first character.
    """
    # TODO
    raise NotImplementedError
